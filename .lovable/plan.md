## Проблема

В файле `src/components/tests/Grade8TechnologyFinalQ4Theory.tsx` в массиве `TASKS` каждый вопрос второй части содержит блок «Что требуется в ответе: …» — это фактически готовый правильный ответ, и он рендерится ученикам прямо под условием вопроса. Мой косяк, согласен.

## Что сделаю

1. В `Grade8TechnologyFinalQ4Theory.tsx`:
   - Разделю каждый объект `TASKS` на два поля: `text` (только условие, видно ученику) и `expected` (эталон, ученику НЕ показывается).
   - В JSX буду рендерить только `text`. Поле `expected` оставлю в коде как ориентир для учителя при ручной проверке (в комментарии или отдельном экспорте, не в UI).

2. Проверю остальные «итоговые контрольные», нет ли там той же утечки эталонов в условии:
   - `Grade5TechnologyFinalQ4V2.tsx`
   - `Grade6TechnologyFinalQ4.tsx`
   - `Grade7TechnologyFinalQ4.tsx`
   - `Grade8PhysicsFinalQ4.tsx`
   
   Если найду — поправлю аналогично.

3. Часть 1 (квиз) не трогаю — там `correct` приходит только в JS, в DOM не рендерится, всё ок.

## Файлы

- `src/components/tests/Grade8TechnologyFinalQ4Theory.tsx` — основная правка
- возможно другие `*FinalQ4*.tsx` по результатам проверки

Изменения только во фронте, миграции и edge-функции не нужны.