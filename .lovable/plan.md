

## План

В `Index.tsx` для контрольной 7 класса «Работа и Мощность» передавать в `<Quiz />` и `<QuizIntro />` параметр `secondsPerQuestion={30}` вместо 20.

Квиз для 9 класса (атом) остаётся на 20 сек.

| Действие | Файл |
|----------|------|
| Изменить | `src/pages/Index.tsx` (передать 30 сек только для `work-power`) |

