# Обновление каталога тестов: 5, 6, 7, 9 классы

## 9 класс

### Физика
В каталоге `src/pages/Index.tsx`:
- **Убрать** `default` («Контрольная №1. Механика, волны, оптика»).
- **Оставить и подсветить как активную** `atom` («Контрольная №2. Атом и атомное ядро (с квизом)»).
- Оставить `final-q4-quiz` (итоговая годовая, 🌟).
- DB-тесты «Атом и ядерная физика» и «быстрык квиз…» подгружаются автоматически.

В БД: исправить опечатку в названии теста `7398da28-f33b-48ab-8f71-7323854a87ab`:
- было: `быстрык квиз по энергии и дефекту массы`
- станет: `Быстрый квиз по энергии и дефекту массы`

(Тексты вопросов проверил — опечаток в них нет.)

Итог: 4 работы — Итоговая Q4, Контрольная №2 (Атом), Быстрый квиз энергии/дефект массы (DB), Атом и ядерная физика (DB).

### Информатика
Ничего не меняем — каталог уже пуст, в БД одна опубликованная «Самостоятельная работа №2…».

### Технология
Без изменений.

## 7 класс

### Информатика
- Каталог: оставить `final-q4-quiz` (🌟 Итоговая годовая).
- БД: переименовать тест `ae185631-f21e-4bf7-841c-5d951dd72522`
  - было: `Итоговая контрольная. 4 четверть. Алфавитный подход`
  - станет: `Самостоятельная работа. 4 четверть. Алфавитный подход`

### Физика
- **Убрать** `default` («Контрольная №1. Давление, Архимедова сила»).
- Оставить `work-power` (Контрольная №2).
- **Добавить заглушку** `final-q4-stub` с заголовком `🌟 Итоговая контрольная за 4 четверть (скоро)`, делаем её **некликабельной** (disabled, серым, текст «Задание появится позже»).
- DB-тесты «Разминка: Правило моментов» и «Самостоятельная: Мощность…» подгружаются автоматически.

### Технология
Без изменений.

## 6 класс
Проверил — каталог + DB корректно показывают 2 DB-теста (Имена и классификация, Быстрый тест №3) + 2 хардкод-итоговые. Без изменений.

## 5 класс
- Каталог: оставить только `final-q4-v3` (как сейчас).
- **Включить подгрузку DB-тестов для 5 класса** (убрать `if (grade === "5") { setDbTests([]); }` в `useEffect`).
- В UI ученика появится дополнительно DB-тест «Итоговая контрольная работа по технологии. 5 класс» (уже опубликован, 9 сдач).

## Технические детали

**Файлы:**
- `src/pages/Index.tsx` — правки `TESTS_CATALOG` (9 физика, 7 физика, 5 не трогаем) + удаление блокировки `dbTests` для 5 класса. Добавление визуальной «заглушки» для `7_physics_final-q4-stub` в рендере списка тестов и в логике выбора (некликабельная карточка).
- Миграция БД: 2 UPDATE на `tests.title` для `7398da28…` и `ae185631…`.

**Заглушка для 7 физики**: добавить отдельный рендеринг — если `entry.id === "final-q4-stub"`, рисуем `<Card>` с `opacity-60` и кнопкой `disabled` с подписью «Задание появится позже». Логику в `TESTS_WITH_QUIZ` / runner не трогаем.

**Подсветка «Контрольной №2» (9 физика)**: добавить `ring-2 ring-primary` или badge «Сейчас сдаём» по `id === "atom"` (через простой массив `ACTIVE_IDS`).

Сохранение данных учеников, запись экрана, зеркала GitHub/GitLab — не затрагиваются.