

Пароль и логин получил. План остаётся тот же, что я предложил выше — добавляю только уточнения:

### Учётные данные
- Логин: `BestTeatcher`
- Пароль: `3vV58#^aGnMNLR`

Сохраню как **два Supabase secret'а**:
- `TEACHER_LOGIN` = `BestTeatcher`
- `TEACHER_PASSWORD` = `3vV58#^aGnMNLR`

Запрошу через add_secret tool сразу при старте реализации — ты вставишь значения в появившиеся поля. В коде в открытом виде нигде не светятся.

### Форма входа
На страницах `/admin` и `/replay/:id` будет форма с двумя полями (логин + пароль) вместо одного. Edge function `verify-teacher-credentials` сравнивает оба значения с secret'ами, при успехе выдаёт HMAC-токен на 8 часов в `sessionStorage`.

### Всё остальное — без изменений
Базовый план 1+3 (event log + rrweb) + оптимизации #1–#6 (сжатие, пароль, индикатор, чанки, админка, timeline) — реализую одним заходом, как договорились.

### От тебя больше ничего не нужно
Только подтверди этот план — переключаюсь в режим редактирования, запрашиваю secret'ы, ты их вставляешь, я пишу код.

