
Да, проблему нашёл. От тебя ничего не требуется — нужен твой approve этого плана, дальше всё делаю я.

## Что было

9 учеников сдавали тест **до** того, как применилась RLS-миграция для storage. У бакета `rrweb-sessions` не было SELECT/UPDATE policies → `upsert` падал → ни одного файла не загрузилось → `replay_url` пустой → в админке кнопка «Запись» открывает пустоту. **Эти 9 записей невосстановимы** (видео нет, только текст ответов и лог нарушений в БД — это всё доступно).

После миграции твой собственный прогон записался успешно (3 чанка в storage, плеер работает). То есть **сейчас система рабочая** для будущих сдач.

## Что укрепить, чтобы не повторилось

1. **Fallback без gzip** в `useRrwebRecorder.ts` — если у ученика старый Safari/Android-Chrome без `CompressionStream`, отправлять чанки как обычный JSON, а не падать молча.
2. **Алерт в Telegram** если на финализации `uploadedChunks === 0` — узнаешь сразу, а не через сутки в админке.
3. **Поддержка обоих форматов** в плеере (`Replay.tsx` + `replay-signed-url`) — уметь читать и `.json.gz`, и `.json`.

## Файлы

| Файл | Что меняется |
|------|--------------|
| `src/hooks/useRrwebRecorder.ts` | try/catch вокруг `CompressionStream`, fallback на raw JSON, алерт через `notify-copy-attempt` если 0 chunks |
| `supabase/functions/replay-signed-url/index.ts` | листинг и подпись `.json` чанков наравне с `.json.gz` |
| `src/pages/Replay.tsx` | `ungzipToJson` уже умеет оба формата (по расширению), проверю |

## Что НЕ делаю
- Не трогаю восстановление 9 записей — это невозможно, файлов нет физически.
- Не трогаю auto-save и typing speed — они работают (auto-save в localStorage на каждом изменении, скорость печати видна в rrweb по timestamp событий).
- Не трогаю кнопку «Выйти» — починена в прошлом ходу.

После approve — внесу 3 правки и попрошу прогнать ещё один тестовый сабмит для проверки.
