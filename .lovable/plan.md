## Что меняем

### 1. Убираем из доступа учеников всё с пометкой «3 четверть»

В `src/pages/Index.tsx` → `TESTS_CATALOG` удаляю строки с `title` содержащим «(3 четверть)»:

- 7 информатика → `default` («Итоговая контрольная (3 четверть)»)
- 7 технология → `default`
- 8 информатика → `default`
- 9 информатика → `default`
- 9 технология → `default`

Старые контрольные по физике (давление/Архимед, работа/мощность, электричество, Джоуль-Ленц, механика, атом и т.п.) **остаются** — вы попросили убрать только явные «3 четверть».

### 2. Пятый класс

Оставляю только Вариант 3 (как сейчас). В коде есть также `Grade5TechnologyFinalQ4V2`, но в каталоге его нет — ничего не трогаю.

> Вы упомянули «там вроде ещё какой-то квиз был другой» — кроме V2/V3 итоговой Q4 в проекте для 5 класса других тестов не нашёл. Если имеется в виду конкретная работа — скажите название, добавлю отдельно.

### 3. Подсвечиваем актуальные работы с 6 апреля

Все DB-тесты (созданные в Кабинете учителя через `loadPublishedTestsForGradeSubject`) рендерятся компонентом `DbTestRunner`-карточкой в списке. Добавлю:

- Если `created_at >= 2026-04-06` → бейдж «🔥 Актуальное · 4 четверть» рядом с названием + рамка с акцентным цветом (`border-primary`, мягкое свечение).
- Хардкод-тесты с эмодзи 🌟 в названии (все Q4) — тоже получают акцентный бордер, чтобы визуально были в одной группе.
- Сверху списка — мини-плашка «Сдавайте работы 4 четверти ↓».

### Технические детали

Файлы:
- `src/pages/Index.tsx` — правка `TESTS_CATALOG` (удаление 5 строк), визуальная подсветка актуальных тестов в списке выбора.

Никаких миграций БД и изменений edge-функций. Опубликованные DB-тесты не трогаем — они уже доступны, просто визуально выделим свежие.

### Что НЕ делаем

- Не удаляем сами компоненты тестов (`Grade7Informatics.tsx` и т.д.) и не снимаем DB-тесты с публикации — старые результаты и replay по ним продолжают открываться в админке.
- Сами квизы за 3 четверть в `quizRegistry` остаются (нужны для отображения старых результатов).
