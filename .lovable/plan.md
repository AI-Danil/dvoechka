## Что меняем — 8 класс + зеркала

### 1. 8 информатика — оставить ровно 3 работы

Сейчас в каталоге (`TESTS_CATALOG["8"]`) уже только `python-hero` (Самостоятельная №2). Остальные 2 работы приходят из БД (опубликованные тесты учителя):

| Тест в БД | Кол-во вопросов | Сданных попыток | Статус |
|---|---|---|---|
| `ТестКвиз01` (f823…) | 7 | **3** | оставить — это рабочий |
| `Тест квиз01` (4596…) | 7 | 0 | **снять с публикации** (дубликат, никто не сдавал) |
| `Итоговая контрольная работа за 4 четверть. Информатика (Python + Turtle)` (8b66…) | — | — | оставить |

Дубликат `4596d3df-…` снимем с публикации одной миграцией: `update public_tests set is_published = false where id = '4596d3df-301a-4606-802d-c388472e8b10';`. Прошлые результаты не трогаем (их и нет).

«Контрольной за 3 четверть» по информатике в каталоге уже нет (удалили в прошлый раз). Ничего дополнительно убирать не нужно.

### 2. 8 физика — без изменений

В каталоге уже только два теста: `power-joule` (Контрольная №2. Работа и мощность тока. Закон Джоуля—Ленца) и `final-q4` (Итоговая Q4 с квизом). Запись «3 четверть» отсутствует. DB-тестов для 8-физики не опубликовано. То, что вы видели как «3 четверть», вероятно было до прошлого апдейта — сейчас визуально всё корректно.

### 3. 8 технология — без изменений

Только `final-q4-theory` (🌟 Итоговая Q4 по технологии). Уже подсвечен (есть 🌟 → рамка `ring-primary`).

### 4. Зеркала — оставить только GitHub Pages и GitLab Pages

Удалить:
- `netlify.toml`
- `wrangler.toml`
- `public/_headers` (это конфиг Cloudflare/Netlify заголовков)

Обновить `docs/DEPLOYMENT.md`:
- убрать секции Netlify, Cloudflare Pages, Cloudflare Tunnel;
- оставить GitHub Pages и GitLab Pages;
- в таблице зеркал оставить только две строки.

GitHub Actions (`.github/workflows/deploy-pages.yml`, `mirror-to-gitlab.yml`) и `.gitlab-ci.yml` — без изменений, они нужны.

### 5. Проверка сохранения данных и записи экрана на GitHub/GitLab

Это проверка, без правок кода:

- Сохранение результатов и аплоад файлов идут напрямую в Supabase по `VITE_SUPABASE_URL` — он одинаковый везде, работает с любого зеркала.
- Запись экрана (`useRrwebRecorder` → `update-replay-url` edge-функция → Supabase Storage) — тоже не зависит от хостинга.
- В `vite.config.ts` `base` уже подставляется корректно для GH (`/<repo>/`) и GitLab (корень при unique-domain). Маршруты SPA отрабатывают через `public/404.html`.

Что проверю руками после деплоя: открыть GH-зеркало и GL-зеркало, пройти короткий тест анонимно, убедиться что появилась запись в `test_results` и `replay_url` подгрузился.

### Файлы

- `supabase/migrations/<timestamp>_unpublish_duplicate_quiz01.sql` — снять `4596d3df…` с публикации.
- `netlify.toml` — удалить.
- `wrangler.toml` — удалить.
- `public/_headers` — удалить.
- `docs/DEPLOYMENT.md` — переписать без Netlify/Cloudflare.

### Чего НЕ делаем

- Не трогаем `TESTS_CATALOG` (для 8 класса всё уже правильно).
- Не удаляем компоненты тестов и не чистим старые `test_results`.
- Не трогаем GH/GL workflows и `.gitlab-ci.yml`.
