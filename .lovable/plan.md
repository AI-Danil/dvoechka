## Цель
В 6 классе показывать только предмет «Технология» (скрыть Информатику и Физику).

## Изменение

**`src/pages/Index.tsx`**, строка 67 в `AVAILABLE_TESTS`:
```ts
"6": ["technology"],
```
(сейчас: `["informatics", "technology", "physics"]`)

## Что НЕ трогаем
- `TESTS_CATALOG` (записи под `"6".informatics/physics` просто перестанут показываться, оставляем — пригодятся при возврате).
- Логика тестов, БД, edge-функции, остальные классы.

## Затронутые файлы
- редактируем: `src/pages/Index.tsx` (1 строка)
