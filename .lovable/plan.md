## Гибридный live-тест: 7 класс → Технология

### 1. Тест в `tests`
- `title = "Быстрый тест: информация, память, файлы"`
- `class_id = bb6299d1-...` (7А), `subject_id = e671aa1c-...` (Технология)
- `kind = 'hybrid'`, `status = 'published'`, `time_per_question_sec = 25`
- `author_user_id` = админ, `teacher_id = 31651e5d-...`

### 2. Вопросы в `test_questions`

**Часть 1 — quiz (5 вопросов, по 25 сек):**
1. Свойство «важна и существенна для настоящего» → Актуальность
2. Формула мощности алфавита → N = 2^i
3. Быстрая память, стирается при выключении → ОЗУ
4. Текстовые/графические редакторы, браузеры → Прикладное ПО
5. На что указывает расширение файла → На тип файла и программу

**Часть 2 — written (3 задания, ~120 сек каждое, эталон в `expected_answer`):**
6. 4 информационных процесса + пример обработки.
7. Расчёт I = K · i для 150 символов × 8 бит → 1200 бит = 150 байт (с распиской).
8. Что такое «полное имя файла» + `D:\Lessons\Informatika\prezentatsia.pptx`.

### 3. Live-сессия в `test_sessions`
- `teacher_user_id` = админ (`73057659-...`), чтобы ты сам стартанул.
- `duration_sec = 5×25 + 3×120 ≈ 485` → округляю до **10 минут (600 сек)**.
- `status = 'waiting'`, 4-символьный код. Покажу в ответе.

### Что НЕ трогаю
- Код приложения, схему БД, RLS, edge-функции.

### Как проверишь
- Админ → «🎙 Live-сессии» → «Быстрый тест: информация, память, файлы (7А — Технология)» с кодом.
- ▶ Старт → ученик вводит код, проходит 5 quiz по 25 сек, потом 3 письменных, общий лимит 10 минут.
